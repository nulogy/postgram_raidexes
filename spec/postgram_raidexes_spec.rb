require "spec_helper"

RSpec.describe PostgramRaidexes do
  it "has a version number" do
    expect(PostgramRaidexes::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
